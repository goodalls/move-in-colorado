import React, { Component } from 'react';

export class Login extends Component {
  render() {
    console.log(this.props);
    //need to get url selected to conditional render each login screen........
    return (
      <div>
        <h1>Login Page</h1>
      </div>
    );
  }
}

export default Login;
